---
title: "Commands"
description: "View all Connections command or a single one"
icon: "circle"
---

# Command Syntax

`c.commands`

![Something](/public/commands.png)

<Card title="Aliases" horizontal={true}>
  cmds, cmd, commands
</Card>

<Tip>
  You can use "c.commands `name`" to view a specific command, REPLACE `name`
  with the current command name
</Tip>
<Warning>This command does not have a slash command version</Warning>
